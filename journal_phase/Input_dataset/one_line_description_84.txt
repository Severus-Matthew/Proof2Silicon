Implement a Dafny program that defines a recursive `Factorial` function and an iterative `ComputeFactorial` method, ensuring correctness through invariants and verification of preconditions and postconditions.