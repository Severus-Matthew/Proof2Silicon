1. **Code File 1**: This Dafny code implements the Fibonacci sequence computation, providing both a recursive function `fib` and an iterative method `Fib` to calculate the nth Fibonacci number. The method includes verification conditions to ensure that the computed result matches the expected Fibonacci value.\n\n2. **Code File 2**: The code introduces a list datatype and implements a recursive function `add` that sums the elements of a list of integers, alongside an iterative method `addImp` to achieve the same goal. The method contains verification conditions to ensure the computed sum matches the expected value based on the recursive function.\n\n3. **Code File 3**: This code defines a method `maxArray` that finds the maximum integer value in a non-empty array, using an iterative approach. It verifies that the result is indeed the maximum value through established postconditions and loop invariants during its execution.\n\n4. **Code File 4**: The method `maxArrayReverse` is similar to `maxArray` but traverses the array from back to front to find the maximum integer. It ensures that the invariant and the postconditions hold, thereby verifying that the identified maximum is correct within the context of the array.\n\n5. **Code File 5**: The code contains a recursive function `sum` that computes the sum of the first n natural numbers, and an iterative method `sumBackwards` that accomplishes the same task in a bottom-up fashion. It includes verification mechanisms to ensure that the final computed sum corresponds with the value derived from the recursive approach.