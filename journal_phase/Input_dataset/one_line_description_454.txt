Implement a Dafny program that computes Fibonacci numbers using both a recursive function and an iterative method, ensuring the iterative method produces the same result as the recursive one through specified invariants.