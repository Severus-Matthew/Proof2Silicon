The provided Dafny code consists of several methods that collectively implement a selection sort algorithm. The `swap` method is used to exchange two elements in an array, while the `FindMin` method locates the index of the minimum element within a specified range of the array, ensuring it meets certain preconditions and maintains invariants. The `selectionSort` method utilizes these helper functions to sort the entire array, verifying at each step that the elements are sorted and that the total number of elements remains unchanged throughout the process.