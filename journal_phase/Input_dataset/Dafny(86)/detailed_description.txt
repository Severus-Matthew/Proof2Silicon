The provided Dafny code defines several datatypes (`Abc`, `Def`, and `Ghi`) that represent recursive structures with various underlying collections (sequences, multisets, and sets). Each datatype is accompanied by lemmas that verify specific properties about these structures, ensuring that an instance of the datatype cannot be a self-contained wrapper of itself (e.g., `Wrapper([a])` or `MultiWrapper(multiset{a})`). The lemmas employ assertions and require non-empty collections to maintain consistency and avoid self-referential definitions.