This Dafny code defines a functional list data structure and provides utility functions for operating on it, including calculating its length, checking for membership of an element, and accessing elements by index. The `from_array` method converts an imperative array into a functional list while ensuring the resulting list has the same length and content as the original array. The `Main` method demonstrates this functionality by creating a list and an array, converting the array to a list, and printing both lists for comparison.