The provided Dafny code defines a method called `FindAllOccurrences` that searches for all occurrences of a given substring (`pattern`) within a larger string (`text`) and returns the starting indices as a set. The method includes several assertions and invariants to ensure the correctness of its behavior, which is verified through multiple lemmas aimed at confirming that the substrings match appropriately and that the calculated hash values for the strings are accurate throughout the search process. Additionally, helper functions and lemmas like `RecursiveSumDown` and `LemmaRabinKarp` are employed to facilitate the hash-based matching approach while maintaining rigorous correctness guarantees.