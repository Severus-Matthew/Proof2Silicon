Implement a generic singly linked list in Dafny that utilizes ghost variables and predicates to ensure structural invariants while providing methods for prepending elements, skipping the head, and reversing the list in place.